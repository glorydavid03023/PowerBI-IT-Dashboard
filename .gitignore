# ── Sensitive client / source data — DO NOT publish ────────────────
# Workbooks contain real IRS PII (Config sheet) + client device data
*.xlsm
*.xlsx
*.pptx

# .pbix caches the source data inside it
*.pbix

# Client correspondence / project notes
chat history.txt
description.md

# Backups and extracted internals
*.BACKUP.*
_extract/
_fix/
_pbix/

# OS / tooling cruft
Thumbs.db
.DS_Store
